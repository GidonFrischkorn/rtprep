% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{ez_ddm}
\alias{ez_ddm}
\title{Invert summary statistics into diffusion parameters}
\usage{
ez_ddm(mean_rt, var_rt, accuracy, n_trials, s = 1)
}
\arguments{
\item{mean_rt, var_rt}{Mean and variance of the response times, in seconds.
Wagenmakers et al. define these on \emph{correct} responses; \code{version = "3par"}
of \code{\link[=rt_summary]{rt_summary()}} pools both boundaries, which is equivalent under an
unbiased diffusion and not otherwise. Use \code{version = "4par"} if the
starting point may be off centre.}

\item{accuracy}{Proportion of upper-boundary (correct) responses, in
\verb{[0, 1]}.}

\item{n_trials}{Number of trials the statistics came from. Required: it sets
the size of the edge correction.}

\item{s}{Scaling constant. \code{1} here; Wagenmakers et al. use \code{0.1}. This is a
units convention, not a modelling one — \code{drift} and \code{bound} scale linearly
with \code{s} and \code{ndt} does not, so a drift of 0.1 at \code{s = 0.1} and a drift of
1.0 at \code{s = 1} describe the same process.}
}
\value{
A \code{data.frame} with \code{drift}, \code{bound}, and \code{ndt}, one row per input
element (inputs recycle to a common length). An \code{"edge_corrected"}
attribute flags which rows needed the correction below.
}
\description{
The closed-form EZ-diffusion equations of Wagenmakers, van der Maas and
Grasman (2007): mean response time, response time variance, and accuracy in;
drift rate, boundary separation, and non-decision time out.

Exported so that the whole pipeline-to-parameters check runs with only
\code{rtprep} installed — a reader can screen, aggregate, and estimate without
reaching for a model-fitting package.
}
\details{
The equations divide by \code{logit(accuracy)} and break down at accuracies of 0,
0.5, and 1. Wagenmakers et al.'s edge correction moves the offending value by
\code{1 / (2 * n_trials)}: 1 becomes \verb{1 - 1/(2n)}, 0 becomes \verb{1/(2n)}, and 0.5
becomes \verb{0.5 + 1/(2n)}. It is applied silently, because it is the published
behaviour and a warning per cell would bury a simulation run — but which
cells were corrected comes back in the \code{"edge_corrected"} attribute, so a
script can count them. Note that \code{[}, \code{rbind()}, and every dplyr verb drop
that attribute, so read it off the returned object directly rather than
expecting it to survive a pipeline.

EZ is fragile under contamination: a handful of fast guesses moves the drift
estimate a long way (Ratcliff, 2008). That fragility is the point of the
comparison this package exists to support, not a reason to avoid the
estimator.
}
\examples{
# the worked example from Wagenmakers et al. (2007)
ez_ddm(
  mean_rt = 0.723, var_rt = 0.112, accuracy = 0.802,
  n_trials = 100, s = 0.1
)

}
\references{
Wagenmakers, E.-J., van der Maas, H. L. J., & Grasman, R. P. P. P. (2007). An
EZ-diffusion model for response time and accuracy. \emph{Psychonomic Bulletin &
Review}, \emph{14}(1), 3–22. \doi{10.3758/bf03194023}

Ratcliff, R. (2008). The EZ diffusion method: Too EZ? \emph{Psychonomic Bulletin &
Review}, \emph{15}(6), 1218–1228. \doi{10.3758/pbr.15.6.1218}
}
\seealso{
\code{\link[=rt_summary]{rt_summary()}}, which produces exactly the inputs this takes.
}
