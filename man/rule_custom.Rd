% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extend.R
\name{rule_custom}
\alias{rule_custom}
\title{A screening rule from a function, in one call}
\usage{
rule_custom(
  label,
  fun,
  ...,
  description = NULL,
  reason = "contaminant",
  needs_response = NULL,
  per_trial = character(),
  grouped = FALSE,
  subclass = "custom"
)
}
\arguments{
\item{label}{A string identifying the rule, used as the \code{.rule} column, by
\code{print()}, and by \code{\link[=screen_compare]{screen_compare()}}, which needs the rules it compares to
be named apart. Conventionally \code{"family(setting, setting)"}.}

\item{fun}{The screening function. What it may take is listed under "What the
screening function receives" in \link{extending}; what it may return, under
"What the screening function must return".}

\item{...}{Named parameters stored on the rule and passed to \code{fun} by name.}

\item{description, reason, needs_response, per_trial, grouped}{As in
\code{\link[=new_rule]{new_rule()}}.}

\item{subclass}{The rule family, for the rare case of wanting a class to hang
a method on later. The default is shared by every \code{rule_custom()} rule,
which costs nothing: the function travels on the object, not on the class,
so two rules built this way never collide.}
}
\value{
An object of class
\code{c("rtprep_rule_custom", "rtprep_rule_fun", "rtprep_rule")}.
}
\description{
The short form of \code{\link[=new_rule]{new_rule()}}: pass the function that decides which trials
to keep, and get a rule back. No constructor, no S3 method, nothing to
register. The result goes anywhere a rule goes — \code{\link[=rt_screen]{rt_screen()}}, \code{\link[=rt_keep]{rt_keep()}},
\code{\link[=screen_fits]{screen_fits()}}, \code{\link[=screen_compare]{screen_compare()}}, and inside \code{\link[=rule_all]{rule_all()}} and its
relatives.
}
\examples{
fast <- rule_custom(
  "fast(0.35)",
  function(rt, cut) rt >= cut,
  cut = 0.35,
  description = "Exclude trials faster than 350 ms.",
  reason = "too_fast"
)
fast
rt_screen(rt_example$rt, fast, .by = rt_example$id)

# against one of the rules rtprep ships
screen_compare(
  rt_example$rt,
  list(fast = fast, mad = rule_mad(2.5)),
  .by = rt_example$id
)

}
\seealso{
\link{extending} for the full contract, \code{\link[=new_rule]{new_rule()}} to wrap a rule of
your own in a constructor.
}
