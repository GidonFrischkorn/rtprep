% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate.R
\name{r_contaminated}
\alias{r_contaminated}
\title{Generate response time data with contaminants of known type}
\usage{
r_contaminated(
  n,
  generator = c("ddm", "rdm"),
  process = c("leading_edge", "delay", "informationless", "mixed"),
  rate = 0.05,
  par = list(),
  st0 = 0,
  anticipation_anchor = c("min", "q10"),
  anticipation_depth = 0.3,
  delay_min = 0,
  delay_max = 2,
  lapse_prop = 0,
  mix = c(leading_edge = 1, delay = 1, informationless = 1)/3,
  dt = 0.001
)
}
\arguments{
\item{n}{Number of trials.}

\item{generator}{The clean core. \code{"ddm"} is a first-principles diffusion
(Euler–Maruyama); \code{"rdm"} a racing diffusion — two independent
single-boundary Wald accumulators with a shared bound, exact
inverse-Gaussian draws.}

\item{process}{Which contaminant process replaces the selected trials:
\itemize{
\item \code{"leading_edge"} — anticipations. Uniform in a band around the observed
minimum of the \emph{clean} trials in this call (see
\code{anticipation_anchor}), responding at chance. Anchoring to an observed
quantity rather than to the generating non-decision time keeps the
construct comparable across generators, whose fitted non-decision
times can differ substantially at identical observables.
\item \code{"delay"} — delayed start-ups. A genuine trial plus a uniform shift of
\code{delay_min} to \code{delay_max} seconds; the response — and so the intact
accuracy — is kept.
\item \code{"informationless"} — lapses of evidence quality. The trial is redrawn
from the core with its evidence scaled by \code{lapse_prop} while the speed
of processing is held fixed: \code{lapse_prop = 0} is a complete lapse
(chance accuracy, response times still in the core's range),
\code{lapse_prop = 1} the clean process, and values between are partial
lapses on the same continuum.
\item \code{"mixed"} — each contaminant trial draws its process from \code{mix}.
}}

\item{rate}{Probability that a trial is a contaminant, in \verb{[0, 1)}.}

\item{par}{Generator parameters as a list. For \code{"ddm"}: \code{drift}, \code{bound},
\code{ndt}, \code{zr} (defaults \verb{1.5, 1.2, 0.30, 0.5}). For \code{"rdm"}: \code{drift} as
\code{c(correct, error)}, \code{bound}, \code{ndt} (defaults \verb{c(3, 1.5), 1.5, 0.30}).
The defaults are illustrative, not calibrated; simulation designs should
set parameters from an observable target instead.}

\item{st0}{Range of across-trial variability in non-decision time, in
seconds. The per-trial non-decision time is uniform on \verb{ndt ± st0 / 2} —
the centred parameterization, so the mean response time is invariant and
only the leading edge smears.}

\item{anticipation_anchor}{Observable the anticipation band is centred on:
the minimum (\code{"min"}) or the 10th percentile (\code{"q10"}) of the clean
trials generated in this call.}

\item{anticipation_depth}{Half-width of the anticipation band as a
proportion of the anchor: response times are uniform on
\verb{anchor * (1 ± anticipation_depth)}.}

\item{delay_min, delay_max}{Bounds of the uniform shift, in seconds, for
\code{process = "delay"}.}

\item{lapse_prop}{Evidence-quality proportion for
\code{process = "informationless"}; see above.}

\item{mix}{Relative weights of \code{leading_edge}, \code{delay}, and
\code{informationless} for \code{process = "mixed"}, in that order. Normalized
internally.}

\item{dt}{Euler–Maruyama step size for the \code{"ddm"} generator, in seconds.
Smaller steps are slower and more faithful: a discretised first-passage
sampler misses boundary crossings that happen inside a step, which
shifts the response times slightly late. The default of one millisecond
was validated against \code{rtdists::rdiffusion()} in the package's tests,
where the two agree in accuracy and in the response time quantiles.}
}
\value{
A \code{data.frame} with \code{n} rows:
\itemize{
\item \code{rt} — response time in seconds,
\item \code{response} — \code{1} for a correct (upper-boundary / winning-accumulator)
response, \code{0} otherwise,
\item \code{contaminant} — logical ground truth,
\item \code{process} — \code{"clean"} or the generating contaminant process per trial.
}
}
\description{
Draws trials from an evidence accumulation core and replaces a known subset
with contaminants generated by one of three psychologically motivated
processes. The ground truth comes back with the data, which is the point:
contamination is invisible in real data, so only generated data with known
truth lets a preprocessing pipeline be tested rather than trusted.
}
\section{Reproducibility}{
There is no \code{set.seed()} anywhere in \code{rtprep}; reproducibility belongs to
the caller.
}

\section{The three processes are clusters, not point definitions}{
Each named process is one implementation of a psychological cluster —
premature responding, late starts, disengagement — and the arguments
(\code{anticipation_anchor}, \code{anticipation_depth}, \code{delay_min}/\code{delay_max},
\code{lapse_prop}) sweep within the cluster. Which implementations a simulation
uses, and how many per cluster, is a design decision for that simulation to
record and defend, not a package default.
}

\examples{
set.seed(1)
d <- r_contaminated(
  200,
  generator = "ddm", process = "leading_edge", rate = 0.1,
  par = list(drift = 1.5, bound = 1.2, ndt = 0.30)
)
table(d$process)
# detection scored against ground truth
scr <- rt_screen(d$rt, rule = rule_sd(2.5))
table(flagged = !scr$.keep, truth = d$contaminant)

}
\references{
Ratcliff, R. (1993). Methods for dealing with reaction time outliers.
\emph{Psychological Bulletin}, \emph{114}(3), 510–532.
\doi{10.1037/0033-2909.114.3.510}

Ratcliff, R., & Tuerlinckx, F. (2002). Estimating parameters of the
diffusion model: Approaches to dealing with contaminant reaction times and
parameter variability. \emph{Psychonomic Bulletin & Review}, \emph{9}(3), 438–481.
\doi{10.3758/bf03196302}
}
\seealso{
\code{\link[=rt_screen]{rt_screen()}} for what the flags make of these trials,
\code{\link[=rt_summary]{rt_summary()}} and \code{\link[=ez_ddm]{ez_ddm()}} for what the contamination does to the
parameters.
}
