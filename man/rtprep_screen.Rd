% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screen-print.R
\name{rtprep_screen}
\alias{rtprep_screen}
\alias{format.rtprep_screen}
\alias{print.rtprep_screen}
\alias{as.data.frame.rtprep_screen}
\alias{[.rtprep_screen}
\title{Methods for a screening result}
\usage{
\method{format}{rtprep_screen}(x, ...)

\method{print}{rtprep_screen}(x, n = 6L, ...)

\method{as.data.frame}{rtprep_screen}(x, row.names = NULL, optional = FALSE, ...)

\method{[}{rtprep_screen}(x, i, j, drop = TRUE)
}
\arguments{
\item{x}{A screening result from \code{\link[=rt_screen]{rt_screen()}}.}

\item{...}{Ignored.}

\item{n}{Number of trials to show. \code{print()} shows the screen summary in
full whatever this is.}

\item{row.names, optional}{Passed to \code{\link[base:as.data.frame]{base::as.data.frame()}}.}

\item{i, j}{Row and column indices.}

\item{drop}{Whether to drop to a vector when one column is selected.}
}
\value{
\code{print()} returns \code{x} invisibly. \code{format()} returns the summary as a
character vector. \code{as.data.frame()} returns a plain \code{data.frame}.
}
\description{
\code{\link[=rt_screen]{rt_screen()}} returns one row per trial with the per-group diagnostics
attached as an attribute. \code{print()} reports what was removed and why before
the rows, because the count is usually the answer wanted and the rows are
usually too many to read.
}
\details{
The \code{fits} attribute describes the screen, so it survives column subsetting
and is dropped by row subsetting: after \code{scr[scr$.keep, ]} its \code{n_dropped}
and \code{prop_dropped} count rows that are no longer there, and carrying it along
would attach a table that quietly disagrees with the object. The class
survives for as long as the four columns do.

The \code{rule} and \code{group_names} attributes, which \code{\link[=report_screening]{report_screening()}} reads,
name the screen rather than count it, so they survive row subsetting too.

\code{as.data.frame()} strips the class and the attributes, for when a plain frame
is wanted.
}
\examples{
scr <- rt_screen(rt_example$rt, rule_mad(2.5), .by = rt_example$id)
scr

# the per-group diagnostics the summary points at
head(attr(scr, "fits"))

}
