% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screen.R
\name{screen_fits}
\alias{screen_fits}
\title{Per-group fit diagnostics from a screening rule}
\usage{
screen_fits(
  rt,
  rule,
  response = NULL,
  .by = NULL,
  policy = c("threshold", "probabilistic"),
  threshold = 0.5
)
}
\arguments{
\item{rt}{Numeric vector of response times \strong{in seconds}. \code{NA} is allowed;
non-positive values are an error.}

\item{rule}{A rule object; see \link{rules}.}

\item{response}{Optional response coding of the same length as \code{rt}, given as
numeric 0/1, logical, or a character or factor using labels such as
\code{"correct"}/\code{"error"} or \code{"upper"}/\code{"lower"}. Required by \code{\link[=rule_ewma]{rule_ewma()}} and
by \code{\link[=rule_mixture]{rule_mixture()}} with \code{use_accuracy = TRUE}.}

\item{.by}{Optional grouping of the same length as \code{rt} — a vector, factor,
list of vectors, or data frame. Rules are fitted separately within each
group. \code{NULL} treats all trials as one group.}

\item{policy}{Exclusion policy. \code{"threshold"} keeps a trial when its
probability of validity exceeds \code{threshold}. \code{"probabilistic"} keeps it
with that probability, drawing once per trial.}

\item{threshold}{Cut for \code{policy = "threshold"}, in \verb{[0, 1]}. Ignored under
the probabilistic policy.}
}
\value{
A \code{data.frame} with one row per group, in group order: \code{.group},
\code{n_trials}, \code{n_dropped}, \code{prop_dropped}, then the rule's own columns
(bounds, criterion, iterations, EM convergence, and so on; see \link{rules}).
\code{n_dropped} and \code{prop_dropped} count under the stated \code{policy} and
\code{threshold}, exactly as \code{attr(rt_screen(...), "fits")} would.
}
\description{
The \code{fits} table of \code{\link[=rt_screen]{rt_screen()}} on its own: one row per group with the
bookkeeping columns and whatever the rule reports. It exists because an
attribute does not survive \code{dplyr::mutate()}, so the table is otherwise out
of reach inside a pipeline.
}
\examples{
rt <- c(0.12, 0.31, 0.35, 0.38, 0.42, 0.47, 0.55, 2.90)
id <- rep(c("a", "b"), each = 4)
screen_fits(rt, rule_sd(2), .by = id)

# with dplyr: dat |> reframe(screen_fits(rt, rule_sd(2.5)), .by = id)

}
\seealso{
\code{\link[=rt_screen]{rt_screen()}}, whose per-trial result carries this table as an
attribute; \code{\link[=screen_compare]{screen_compare()}} for the same table stacked over several
rules.
}
