% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rules.R
\name{rules}
\alias{rules}
\alias{rule_cutoff}
\alias{rule_sd}
\alias{rule_mad}
\alias{rule_recursive}
\alias{rule_ewma}
\alias{rule_mixture}
\alias{rule_none}
\alias{print.rtprep_rule}
\title{Screening rules}
\usage{
rule_cutoff(min = 0, max = Inf)

rule_sd(n_sd = 2.5, center = c("mean", "median"), scale = c("sd", "mad"))

rule_mad(n_mad = 2.5)

rule_recursive(type = c("moving", "modified", "hybrid"), include_max = TRUE)

rule_ewma(lambda = 0.01, L = 1.5, chance = 0.5)

rule_mixture(
  distribution = c("exgaussian", "lognormal", "invgaussian"),
  bound = c("min", "max"),
  use_accuracy = FALSE,
  chance = 0.5,
  init = 0.05,
  max_prop = 0.5,
  maxit = 100,
  tol = 1e-06
)

rule_none()

\method{print}{rtprep_rule}(x, ...)
}
\arguments{
\item{min, max}{Absolute bounds in seconds. Bounds are \strong{inclusive}: a trial
is flagged only when it falls strictly outside. \code{max = Inf} leaves the
upper tail untouched.}

\item{n_sd, n_mad}{Multiplier applied to the spread statistic.}

\item{center}{Location statistic, \code{"mean"} or \code{"median"}.}

\item{scale}{Spread statistic, \code{"sd"} or \code{"mad"}. \code{"mad"} uses \code{\link[stats:mad]{stats::mad()}}
and so carries the usual consistency constant of 1.4826.}

\item{type}{Which recursive criterion to use; see Details.}

\item{include_max}{Whether the largest response time enters the mean and
standard deviation from which the criterion is built. Applies to
\code{type = "moving"} only.}

\item{lambda}{Smoothing weight of the exponentially weighted moving average,
in \verb{(0, 1]}. Smaller values average over more trials.}

\item{L}{Control-limit multiplier; the chart signals when the average
departs from chance by more than \code{L} standard errors.}

\item{chance}{Accuracy expected from a contaminant response, known from the
design (0.5 for a two-alternative task).}

\item{distribution}{Parametric distribution for the valid response time
component of the mixture.}

\item{bound}{Length-2 bounds of the uniform contaminant component. Each
element may be a number, or \code{"min"}/\code{"max"} for a data-driven bound.}

\item{use_accuracy}{Whether accuracy enters the mixture likelihood.
\strong{Experimental}; see Details.}

\item{init}{Starting value for the contaminant proportion.}

\item{max_prop}{Upper bound on the estimated contaminant proportion.}

\item{maxit}{Maximum number of EM iterations.}

\item{tol}{Convergence tolerance on the log-likelihood.}

\item{x}{A rule object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{c("rtprep_rule_<name>", "rtprep_rule")}: a list
of validated parameters plus a \code{label} element used for the \code{.rule} column
of \code{\link[=rt_screen]{rt_screen()}}.
}
\description{
Rule constructors are parameter objects: they validate their arguments and
describe themselves, but perform no computation on data. Pass one to
\code{\link[=rt_screen]{rt_screen()}}, which applies it and returns the same four per-trial columns
whichever rule was used.

Rules from incompatible families therefore become directly comparable:
\itemize{
\item \code{rule_cutoff()} — fixed absolute bounds.
\item \code{rule_sd()} — a location/spread criterion, covering both the SD criterion
and the median absolute deviation criterion.
\item \code{rule_mad()} — a thin alias of \code{rule_sd(center = "median", scale = "mad")}.
\item \code{rule_recursive()} — the sample-size-dependent criteria of van Selst and
Jolicoeur (1994).
\item \code{rule_ewma()} — the accuracy control chart of Vandekerckhove and
Tuerlinckx (2007).
\item \code{rule_mixture()} — a uniform-contaminant mixture fitted by EM.
\item \code{rule_none()} — a pass-through baseline.
}
}
\section{Absolute cutoffs}{
The oldest and still the most common screen: discard anything faster than a
plausible minimum or slower than a plausible maximum. Ratcliff (1993) is the
standard treatment; Ulrich and Miller (1994) is the counterweight, showing
that truncation biases the surviving distribution even when it removes real
contaminants.

Bounds are inclusive, so \code{rule_cutoff(min = 0.18)} keeps a response time of
exactly 180 ms — 180 ms is not \emph{below} 180 ms. Note that \code{trimr} uses strict
comparisons, so the two implementations can disagree on a trial sitting
exactly on a bound.
}

\section{Location and spread criteria}{
\code{rule_sd()} flags trials further than \code{n_sd} spread units from a centre,
recomputed within each group of \code{\link[=rt_screen]{rt_screen()}}. \verb{center = "mean", scale = "sd"} is the standard deviation criterion, modal practice in the field;
\verb{center = "median", scale = "mad"} is the median absolute deviation
criterion recommended by Leys et al. (2013), also available as
\code{rule_mad()}.

One constructor covers both because they are one family — the same algorithm
with a different location/spread pair. Miller (1991) is the standard warning
about the standard deviation criterion: the proportion of a skewed
distribution that survives a fixed multiplier depends on sample size, so the
criterion silently changes what it removes as trial counts vary. That
dependence is what \code{rule_recursive()} was designed to remove.

When the spread statistic cannot be used — fewer than two observed trials in
a group, or zero spread — nothing is flagged. A rule that cannot be evaluated
must not remove data.
}

\section{Recursive and moving criteria}{
Van Selst and Jolicoeur (1994) answered Miller's (1991) sample-size problem
by making the multiplier itself depend on the number of trials. \code{rtprep}
ships their published criterion table for sample sizes 4 to 100; below 4 no
criterion exists and nothing is flagged, and above 100 the value for 100 is
used.
\itemize{
\item \code{type = "moving"} is their non-recursive moving criterion: one pass, with
the multiplier read off the table for the group's trial count.
\item \code{type = "modified"} is the modified recursive procedure: the largest
remaining response time is \emph{temporarily set aside} while the mean and
standard deviation are computed, the most extreme trial at each end is
removed if it falls outside the resulting bounds, and the procedure repeats
until nothing is removed or fewer than five trials remain. The temporary
exclusion is what makes the rule bite, so it applies whatever \code{include_max}
says — \code{include_max} governs \code{type = "moving"} only.
\item \code{type = "hybrid"} averages the two. Per trial, \code{.prob} is the mean of the
two rules' decisions and so takes the value 0, 0.5, or 1; under the default
keep policy a trial survives only if both rules keep it.
}

Note that van Selst and Jolicoeur's published hybrid statistic is the mean of
the two \emph{condition means}, which no single per-trial keep vector can
reproduce, because the two means have different denominators. To recover the
published statistic, summarise the two rules separately and average:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{keep_moving <- rt_screen(rt, rule = rule_recursive("moving"))$.keep
moving <- rt_summary(rt[keep_moving])
keep_modif <- rt_screen(rt, rule = rule_recursive("modified"))$.keep
modif <- rt_summary(rt[keep_modif])
(moving$mean_rt + modif$mean_rt) / 2
}\if{html}{\out{</div>}}
}

\section{Accuracy control chart}{
\code{rule_ewma()} is the exponentially weighted moving average cutoff introduced
with DMAT (Vandekerckhove & Tuerlinckx, 2007), and the only \emph{published}
screen that uses accuracy rather than response time alone. Trials are ordered
from fastest to slowest and an exponentially weighted average of accuracy is
accumulated, starting from \code{chance}. The control limit at trial \eqn{i} is

\deqn{\mathrm{UCL}_i = \gamma + L\,\sigma
  \sqrt{\frac{\lambda}{2-\lambda}\left(1-(1-\lambda)^{2i}\right)},}

with \eqn{\gamma} the chance rate and \eqn{\sigma = \sqrt{\gamma(1-\gamma)}}
the standard deviation of a guess. The cutoff is the response time at which
the average first rises above the limit: below it, accuracy is
indistinguishable from guessing, so those trials are flagged. If the average
never crosses, nothing is flagged.

The chart lags, and the lag is a cost rather than an implementation detail:
the average needs several trials above chance before it clears the limit, so
the cutoff lands past the point where accuracy actually rose and valid trials
just above it are flagged along with the guesses. Smaller \code{lambda} averages
over more trials and overshoots further. Ratcliff and Kang (2021) note that
this rule has not found much use; \code{rtprep} ships it as the published
accuracy-based comparator, not as a recommendation.

This rule requires \code{response}, coded as correct/error rather than as an
upper/lower boundary.
}

\section{Mixture flagging}{
\code{rule_mixture()} fits, per group, a two-component mixture of a uniform
contaminant distribution over \code{bound} and a parametric response time
distribution, by expectation maximisation (Ratcliff & Tuerlinckx, 2002).
\code{.prob} is then the posterior probability that a trial came from the response
time component — the one rule in the package that returns something other
than 0 and 1, and the reason \code{\link[=rt_screen]{rt_screen()}} separates the probability from the
keep decision at all.

\code{use_accuracy = TRUE} is \strong{experimental}. It puts accuracy inside the
mixture likelihood rather than using it only afterwards, on the reasoning
that a fast trial that is \emph{correct} is less likely to be a guess than a fast
trial that is an error. It assumes contaminants respond at \code{chance}, which
holds for guessing but not for a delayed start-up, where the decision process
still runs and accuracy is intact.
}

\section{No screening}{
\code{rule_none()} keeps every trial. It exists so that "no preprocessing" is an
entry in the roster rather than a missing row, and so that pipelines can be
compared against it without a special case.
}

\examples{
rule_cutoff(0.18, 3)

rule_sd(2.5)
rule_mad(3)

rule_recursive("modified")

rule_ewma(lambda = 0.05)

rule_mixture("lognormal")

rule_none()

}
\references{
Ratcliff, R. (1993). Methods for dealing with reaction time outliers.
\emph{Psychological Bulletin}, \emph{114}(3), 510–532.
\doi{10.1037/0033-2909.114.3.510}

Ulrich, R., & Miller, J. (1994). Effects of truncation on reaction time
analysis. \emph{Journal of Experimental Psychology: General}, \emph{123}(1), 34–80.
\doi{10.1037/0096-3445.123.1.34}

Leys, C., Ley, C., Klein, O., Bernard, P., & Licata, L. (2013). Detecting
outliers: Do not use standard deviation around the mean, use absolute
deviation around the median. \emph{Journal of Experimental Social Psychology},
\emph{49}(4), 764–766. \doi{10.1016/j.jesp.2013.03.013}

Miller, J. (1991). Reaction time analysis with outlier exclusion: Bias varies
with sample size. \emph{The Quarterly Journal of Experimental Psychology Section
A}, \emph{43}(4), 907–912. \doi{10.1080/14640749108400962}

Van Selst, M., & Jolicoeur, P. (1994). A solution to the effect of sample
size on outlier elimination. \emph{The Quarterly Journal of Experimental
Psychology Section A}, \emph{47}(3), 631–650. \doi{10.1080/14640749408401131}

Cousineau, D., & Chartier, S. (2010). Outliers detection and treatment: A
review. \emph{International Journal of Psychological Research}, \emph{3}(1), 58–67.
\doi{10.21500/20112084.844}

Vandekerckhove, J., & Tuerlinckx, F. (2007). Fitting the Ratcliff diffusion
model to experimental data. \emph{Psychonomic Bulletin & Review}, \emph{14}(6),
1011–1026. \doi{10.3758/bf03193087}

Ratcliff, R., & Kang, I. (2021). Qualitative speed-accuracy tradeoff effects
can be explained by a diffusion/fast-guess mixture model. \emph{Scientific
Reports}, \emph{11}, 15169. \doi{10.1038/s41598-021-94451-7}

Ratcliff, R., & Tuerlinckx, F. (2002). Estimating parameters of the diffusion
model: Approaches to dealing with contaminant reaction times and parameter
variability. \emph{Psychonomic Bulletin & Review}, \emph{9}(3), 438–481.
\doi{10.3758/bf03196302}
}
\seealso{
\code{\link[=rt_screen]{rt_screen()}} to apply a rule; \code{screen_compare()} to apply several.
}
