% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{check_guessing}
\alias{check_guessing}
\title{Test whether the fast trials a rule removed were really guesses}
\usage{
check_guessing(
  keep,
  rt,
  response,
  threshold_type = c("quantile", "absolute"),
  rt_threshold = 0.25,
  chance = 0.5,
  prior_alpha = 1,
  prior_beta = 1,
  credible_mass = 0.95
)
}
\arguments{
\item{keep}{Logical vector of keep decisions, typically the \code{.keep} column of
\code{\link[=rt_screen]{rt_screen()}}. Note this is the \emph{keep} flag, not a contaminant flag —
\code{rtprep}'s convention throughout — so a screen's output drops straight in.}

\item{rt}{Numeric vector of response times, in seconds.}

\item{response}{Response coding of the same length, in any form
\code{\link[=rt_screen]{rt_screen()}} accepts.}

\item{threshold_type}{Whether \code{rt_threshold} is a quantile of the response
time distribution or an absolute value in seconds.}

\item{rt_threshold}{The cut defining "fast": a quantile in \verb{(0, 1)}, or a
positive number of seconds.}

\item{chance}{Accuracy expected from a guess, known from the design.}

\item{prior_alpha, prior_beta}{Beta prior on the proportion correct. The
default \verb{1, 1} is uniform.}

\item{credible_mass}{Mass of the highest-density interval.}
}
\value{
A one-row \code{data.frame} — a row rather than \code{bmm}'s list, because in
practice this goes into a results table:

\describe{
\item{\code{prop_upper}}{observed proportion correct among the tested trials.}
\item{\code{hdi_lower}, \code{hdi_upper}}{highest-density interval on that
proportion.}
\item{\code{bf_01}}{Savage–Dickey Bayes factor for guessing against not.}
\item{\code{guess_in_hdi}}{whether \code{chance} falls inside the interval.}
\item{\code{bf_evidence}}{the Bayes factor on Jeffreys' scale.}
\item{\code{posterior_alpha}, \code{posterior_beta}, \code{n_tested}, \code{rt_threshold},
\code{threshold_type}, \code{credible_mass}, \code{mean_rt_tested}}{the inputs and
intermediates, so a results table is self-describing.}
}
}
\description{
A screening rule tells you which trials it removed. It does not tell you
whether it was right. This is one check that it was: if the fast trials a
rule excluded really were guesses, their accuracy should be at chance.

A Beta-Binomial test with a Savage–Dickey Bayes factor, ported from
\code{bmm::validate_fast_guesses()}.
}
\details{
The test looks only at trials that were \strong{both excluded and fast}. Slow
exclusions are a different claim — a slow contaminant is an attention lapse,
not a guess, and there is no reason to expect chance accuracy from one.

This is the diagnostic counterpart of \code{rule_mixture(use_accuracy = TRUE)}:
one checks accuracy after flagging, the other uses it during. Given what the
package's own tests found about the latter — that it collapses on overlapping
contamination and actively hurts when contaminants keep their accuracy — this
is currently the safer of the two instruments.

When no trial is both excluded and fast, the row comes back with
\code{n_tested = 0} and \code{NA} statistics rather than an error. In a simulation that
cell is common, and informative: it means the rule removed nothing fast.
}
\examples{
set.seed(3)
rt <- c(runif(20, 0.15, 0.30), rgamma(80, 5, 10) + 0.2)
response <- c(rbinom(20, 1, 0.5), rbinom(80, 1, 0.85))
scr <- rt_screen(rt, rule = rule_cutoff(0.35, 3))

check_guessing(scr$.keep, rt, response)

}
\references{
Jeffreys, H. (1961). \emph{Theory of Probability} (3rd ed.). Oxford University
Press.
}
\seealso{
\code{\link[=rt_screen]{rt_screen()}} for the \code{keep} vector, \code{\link[=rule_ewma]{rule_ewma()}} for a rule that
uses accuracy to screen rather than to check.
}
