% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rules-compose.R
\name{rules_compose}
\alias{rules_compose}
\alias{rule_all}
\alias{rule_any}
\alias{rule_then}
\title{Combine screening rules}
\usage{
rule_all(...)

rule_any(...)

rule_then(..., threshold = 0.5)
}
\arguments{
\item{...}{Two or more rule objects.}

\item{threshold}{Probability above which a trial survives a stage and is
passed to the next. Only \code{rule_then()} takes this: which trials the later
stages see is part of the rule, not part of the exclusion policy applied
afterwards by \code{\link[=rt_screen]{rt_screen()}}.}
}
\value{
An object of class \code{c("rtprep_rule_<name>", "rtprep_rule")} holding
the components in \code{rules}.
}
\description{
A composite is a rule, so it goes wherever a rule goes: \code{\link[=rt_screen]{rt_screen()}},
\code{\link[=rt_keep]{rt_keep()}}, \code{\link[=screen_fits]{screen_fits()}}, \code{\link[=screen_compare]{screen_compare()}}.
}
\details{
\code{rule_all()} keeps a trial only when every component keeps it, so the trials
it removes are the \emph{union} of what the components remove. \code{rule_any()} keeps
a trial that any component keeps, so it removes only the \emph{intersection}.
\code{rule_then()} runs the components in order, each estimated on the trials the
previous ones left.
}
\section{Which one you want}{

Screening rules disagree because they look in different places. The location
and spread criteria and the recursive criteria read the slow tail; on
anticipations at the leading edge of a right-skewed distribution they remove
nothing, because those trials sit well inside a criterion built around the
mean. \code{rule_ewma()} reads accuracy and finds the leading edge, and is blind
to the slow tail. Combining one of each with \code{rule_all()} covers both,
which no single conventional rule does.

\code{rule_then()} is the other idiom, and it is what a two-stage description like
"trials below 200 ms were discarded, then trials more than 2.5 SD from each
participant's mean" actually means: the standard deviation is computed after
the floor, on the survivors. \code{trimr::sdTrim(minRT = , sd = )} does exactly
this, and \code{rule_then(rule_cutoff(0.2), rule_sd(2.5))} reproduces it. Writing
the two stages in parallel instead gives a different answer, because the
fast trials are still inflating the standard deviation when it is estimated.
}

\section{What \code{.prob} becomes}{

\code{rtprep} keeps the probability that a trial is valid separate from the
decision to drop it, and a composite has to preserve that. \code{rule_all()}
takes the smallest of the component probabilities and \code{rule_any()} the
largest, so the composite's \code{.prob} is still a probability of validity and
still drives \code{policy = "probabilistic"} and \code{rt_summary(weights = )}. For
components that return 0 and 1 this reduces to the logical operation, and a
mixture combined with a deterministic rule keeps its posterior wherever the
deterministic rule does not veto.

\code{rule_then()} reports the probability from the stage that flagged the trial,
or from the last stage to see it if none did. \code{.reason} comes from the first
component to flag, in the order given.
}

\section{What \code{screen_fits()} reports}{

One row per group, as always, with each component's diagnostics prefixed by
its position: \code{r1_lower}, \code{r2_criterion}, and so on. \code{rule_then()} adds
\code{r1_n_flagged}, \code{r2_n_flagged}, ... so it is visible how much work each stage
did, which is the number a staged pipeline usually wants and rarely reports.
}

\examples{
# the slow tail and the leading edge, which no single rule covers
rule_all(rule_mad(2.5), rule_ewma())

# a floor, then a criterion estimated on what the floor left
staged <- rule_then(rule_cutoff(0.2), rule_sd(2.5))
staged

screen_fits(rt_example$rt, staged, .by = rt_example$id)

}
\seealso{
\link{rules} for the components.
}
