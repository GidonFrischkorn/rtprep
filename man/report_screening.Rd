% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{report_screening}
\alias{report_screening}
\alias{report_screening.rtprep_screen}
\alias{report_screening.data.frame}
\title{Report a screening procedure in prose}
\usage{
report_screening(x, ...)

\method{report_screening}{rtprep_screen}(x, rule = NULL, groups = NULL, max_words = 300L, ...)

\method{report_screening}{data.frame}(
  x,
  rule,
  groups = NULL,
  policy = c("threshold", "probabilistic"),
  threshold = 0.5,
  max_words = 300L,
  ...
)
}
\arguments{
\item{x}{A screening result from \code{\link[=rt_screen]{rt_screen()}}, or a data frame carrying its
four columns.}

\item{...}{Ignored.}

\item{rule}{The rule that produced the screen. Optional for an
\code{\link[=rt_screen]{rt_screen()}} result, which carries it; required for a data frame, where
it cannot be recovered.}

\item{groups}{Character vector naming the grouping variables, for the
sentence that says what the criterion was computed within. Optional for an
\code{\link[=rt_screen]{rt_screen()}} result when \code{.by} was a data frame or a named list, which
supplies them. For a data frame these must be columns of \code{x}, since their
values are needed to rebuild the cells.}

\item{max_words}{A guard on the length of the paragraph. Optional clauses
are dropped, lowest priority first, rather than any sentence being
truncated.}

\item{policy, threshold}{The exclusion policy the screen was run under. Read
from the object where it carries them; pass them for a data frame if they
were not left at the defaults.}
}
\value{
An object of class \code{rtprep_report}: a list whose \code{text} element is
the paragraph, carrying alongside it every number the paragraph quotes
(\code{n_trials}, \code{n_missing}, \code{n_screened}, \code{n_excluded}, \code{prop_excluded},
\code{reasons}, \code{cells}, \code{words}), the \code{fits} table it read them from, the rule,
the reference keys and their \code{bibliography}, and any \code{notes}.
}
\description{
Turns a screen into a paragraph that can go into a Methods section: which
rule at which setting, the grouping the criterion was computed within, how
much it removed in total and per cell, what it caught, whether it read
accuracy, the keep policy, and the references for the criteria used.

Every number and every description is derived from the screen, so editing the
rule and forgetting to edit the paragraph is not a way to publish a wrong
Methods section.
}
\details{
The counts separate what the rule excluded from what was never screened.
Trials with a missing response time, a missing grouping key, or (for a rule
that reads accuracy) a missing response, come back with \code{.keep = FALSE} and
\code{.reason = "missing"}, so \code{sum(!.keep)} overstates the rule's work. The
percentages quoted for the rule are out of the trials it actually saw, and
the missing trials get their own sentence.
}
\section{Which object to pass}{

\code{rt_screen()} attaches the rule and the grouping to its result, so
\code{report_screening(scr)} needs nothing else. Inside
\code{dplyr::mutate(rt_screen(rt, rule), .by = ...)} the four columns are spliced
into the data frame and the attributes are lost, so the data frame method
asks for the \code{rule} back and for \code{groups}, the names of the grouping
columns. It recomputes the per-cell counts from \code{.keep} and those columns,
which needs no refitting and gives the same answer.
}

\section{What it does not claim}{

The reference list names the sources for the criteria used and the standard
caveats on them, which are not the same thing: \code{rule_sd()} cites Miller
(1991) on sample-size bias, and \code{rule_mad()} also cites Leys et al. (2013),
whose argument is that the mean and standard deviation are the wrong choice.
Read the sentence as a pointer to the literature, not as an endorsement.
\code{toBibtex()} on the result gives the entries.
}

\examples{
scr <- rt_screen(rt_example$rt, rule_mad(2.5), .by = rt_example$id)
report_screening(scr, groups = "participant")

# every number in the paragraph, for a sentence written by hand
rep <- report_screening(scr)
rep$n_excluded
rep$cells

}
\seealso{
\code{\link[=rt_screen]{rt_screen()}} for the screen, \code{\link[=screen_fits]{screen_fits()}} for the per-cell table
the counts come from, and \link{rtprep_report} for the print and BibTeX methods.
}
