% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{rtprep_report}
\alias{rtprep_report}
\alias{format.rtprep_report}
\alias{print.rtprep_report}
\alias{as.character.rtprep_report}
\alias{toBibtex.rtprep_report}
\title{Methods for a screening report}
\usage{
\method{format}{rtprep_report}(x, width = 0.9 * getOption("width"), ...)

\method{print}{rtprep_report}(x, ...)

\method{as.character}{rtprep_report}(x, ...)

\method{toBibtex}{rtprep_report}(object, ...)
}
\arguments{
\item{x, object}{A report from \code{\link[=report_screening]{report_screening()}}.}

\item{width}{Wrapping width for \code{print()}.}

\item{...}{Ignored.}
}
\value{
\code{format()} returns the wrapped paragraph as a character vector,
\code{print()} returns \code{x} invisibly, \code{as.character()} returns the paragraph as
a single string, and \code{toBibtex()} returns the BibTeX entries for the
references cited.
}
\description{
\code{\link[=report_screening]{report_screening()}} returns the paragraph together with every number in it.
\code{print()} wraps the paragraph to the console width and says how long it is;
\code{as.character()} returns it unwrapped, for pasting into a document or an
inline \code{r} chunk.
}
\examples{
scr <- rt_screen(rt_example$rt, rule_mad(2.5), .by = rt_example$id)
rep <- report_screening(scr, groups = "participant")
rep

cat(as.character(rep))
utils::toBibtex(rep)

}
