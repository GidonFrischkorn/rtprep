% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screen.R
\name{rt_keep}
\alias{rt_keep}
\title{Keep vector from a screening rule}
\usage{
rt_keep(
  rt,
  rule,
  response = NULL,
  .by = NULL,
  policy = c("threshold", "probabilistic"),
  threshold = 0.5,
  quiet = FALSE
)
}
\arguments{
\item{rt}{Numeric vector of response times \strong{in seconds}. \code{NA} is allowed;
non-positive values are an error.}

\item{rule}{A rule object; see \link{rules}.}

\item{response}{Optional response coding of the same length as \code{rt}, given as
numeric 0/1, logical, or a character or factor using labels such as
\code{"correct"}/\code{"error"} or \code{"upper"}/\code{"lower"}. Required by \code{\link[=rule_ewma]{rule_ewma()}} and
by \code{\link[=rule_mixture]{rule_mixture()}} with \code{use_accuracy = TRUE}.}

\item{.by}{Optional grouping of the same length as \code{rt} — a vector, factor,
list of vectors, or data frame. Rules are fitted separately within each
group. \code{NULL} treats all trials as one group.}

\item{policy}{Exclusion policy. \code{"threshold"} keeps a trial when its
probability of validity exceeds \code{threshold}. \code{"probabilistic"} keeps it
with that probability, drawing once per trial.}

\item{threshold}{Cut for \code{policy = "threshold"}, in \verb{[0, 1]}. Ignored under
the probabilistic policy.}

\item{quiet}{If \code{FALSE} (the default), one message states the rule, the
number of trials dropped, and the proportion. \code{TRUE} suppresses it.}
}
\value{
A logical vector the length of \code{rt}: \code{TRUE} for a trial to keep.
Trials with a missing response time or grouping key are \code{FALSE}, as in
\code{\link[=rt_screen]{rt_screen()}}.
}
\description{
The \code{.keep} column of \code{\link[=rt_screen]{rt_screen()}} on its own, for the case where a filter
is all that is wanted. It reports how many trials it dropped, once per call,
so that the exclusion count is logged next to the exclusion rather than
reconstructed afterwards.
}
\details{
Inside a grouped \code{filter()} the message fires once per group, because the
function is called once per group. Pass \code{.by} to \code{rt_keep()} instead of to
\code{filter()}: the keep vector is identical either way, and the count then
covers the whole data set in one line. Or set \code{quiet = TRUE}.
}
\examples{
rt <- c(0.12, 0.31, 0.35, 0.38, 0.42, 0.47, 0.55, 2.90)
keep <- rt_keep(rt, rule_cutoff(0.18, 2.5))
rt[keep]

# with dplyr: dat |> filter(rt_keep(rt, rule_sd(2.5), .by = id))

}
\seealso{
\code{\link[=rt_screen]{rt_screen()}} for the probability and the reason alongside the
decision; \code{\link[=screen_fits]{screen_fits()}} for the per-group diagnostics.
}
