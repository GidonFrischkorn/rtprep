% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{rt_example}
\alias{rt_example}
\title{Simulated response times from four participants, with ground truth}
\format{
A \code{data.frame} with 800 rows and 9 columns:

\describe{
\item{\code{id}}{participant, \code{"p1"} to \code{"p4"}.}
\item{\code{condition}}{factor, \code{"hard"} or \code{"easy"}; the base drifts the
two conditions are built from differ by 0.4, before the participant's
ability factor multiplies both (see Details).}
\item{\code{trial}}{trial number within the cell, 1 to 100.}
\item{\code{rt}}{response time in seconds.}
\item{\code{response}}{\code{1} for a correct response, \code{0} for an error.}
\item{\code{contaminant}}{logical ground truth.}
\item{\code{process}}{\code{"clean"}, \code{"leading_edge"}, \code{"delay"}, or
\code{"informationless"}.}
\item{\code{true_drift}}{the drift the cell was generated from.}
\item{\code{contam_rate}}{the participant's contamination rate: \code{.02}, \code{.05},
\code{.10}, and \code{.15} for \code{p1} to \code{p4}.}
}
}
\usage{
rt_example
}
\description{
A small data set for the examples and the vignette: four participants, two
conditions, 100 trials per cell, generated by \code{\link[=r_contaminated]{r_contaminated()}} from a
diffusion core with contaminants from all three processes. Because the data
are simulated, every trial carries the truth a real data set cannot: whether
it was a contaminant, and which process produced it.
}
\details{
The participants differ in two things at once. Their drift differs, by a
fixed ability factor of \code{0.85}, \code{0.95}, \code{1.05}, and \code{1.20} on a base drift
of \code{1.5} (hard) and \code{1.9} (easy), with \code{bound = 1.2} and \code{ndt = 0.30}
throughout. And their contamination rate differs, so that the error in a
participant's estimate can be set against their own rate. That comparison is
the question the package's companion tutorial asks of every preprocessing
pipeline.

The generating script is \code{data-raw/rt_example.R} in the source repository;
the seed is fixed there, not in the package.
}
\examples{
head(rt_example)
table(rt_example$id, rt_example$process)

}
\seealso{
\code{\link[=r_contaminated]{r_contaminated()}} to generate data shaped like your own task.
}
\keyword{datasets}
